import 'package:flutter/material.dart';

class Dashboardpage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Dashboard Page'),
      ),
      body: Center(
        child: Text('Here is DashboardPage'),
      ),
    );
  }
}
